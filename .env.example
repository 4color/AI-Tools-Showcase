# Docker Compose 可选环境变量（复制为 .env 后按需修改）
# 单镜像：app = 前端(Nginx) + 后端(Java)，对外只暴露 80

# MySQL
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=ai_tools_showcase
MYSQL_PORT=3306

# 应用（单镜像，端口映射 3000:80）
APP_PORT=3000
SPRING_PROFILES_ACTIVE=prod
JWT_SECRET=aiToolsShowcaseSecretKey2024ForJwtTokenGenerationMustBe256Bits
